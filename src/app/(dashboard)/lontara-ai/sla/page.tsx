import { PlaceholderPage } from "@/components/placeholder";

export default function Page() {
  return (
    <PlaceholderPage
      title="Lontara AI — SLA Prediction"
      description="Prediksi SLA aduan menggunakan IndoBERT + XGBoost (Lontara AI). Akan dipindahkan dari dashboard-aduan-ai.php."
      backTo="/"
    />
  );
}