import { PlaceholderPage } from "@/components/placeholder";

export default function Page() {
  return (
    <PlaceholderPage
      title="Dashboard Event — Rekapitulasi"
      description="Rekapitulasi registrasi event (dashboard-rekap-event). Akan dipindahkan pada tahap berikutnya."
      backTo="/"
    />
  );
}
